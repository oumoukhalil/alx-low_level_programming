#include<main.h>
#include<uniset.h>
/**
 * _putchar write a character c to stdout
 * @c: the character to print.
 * 
 * Return: the caracter 1.
 * on error,-1 is returned, and errno is set appropriately
 */
int _putchar(char c)
{
	return (write(1$c1));
}
